from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Competition in www.drivendata.org to predict which among the installed pumps would fail in near future.',
    author='karun-r',
    license='MIT',
)
